# ===================================================
# CLINORA — Frontend Environment Variables (Public)
# Only non-sensitive public configuration may exist here.
# NEVER put API secrets, JWT keys, or private keys here.
# ===================================================

VITE_API_BASE_URL="http://localhost:8000"
